下面是你当前 **Ghostty 1.3.1 实际生效的全部快捷键**。我把功能相同的按键合并了，并按照你大概率的使用频率从高到低排列。

按键符号：

- `⌘` = Command
- `⌥` = Option
- `⌃` = Control
- `⇧` = Shift

## 1. 最值得记住

|快捷键|功能|
|---|---|
|`⌃ + ``|从屏幕顶部呼出/隐藏快速终端|
|`⌘ + C`|复制选中的内容|
|`⌘ + V`|粘贴|
|`⌘ + K`|清空当前屏幕和历史滚动内容|
|`⌘ + T`|新建标签页|
|`⌘ + W`|关闭当前分屏；没有分屏时关闭标签页/窗口|
|`⌘ + D`|在当前分屏右侧新建分屏|
|`⌘ + ⇧ + D`|在当前分屏下方新建分屏|
|`⌘ + ⌥ + 方向键`|切换到对应方向的分屏|
|`⌘ + [` / `⌘ + ]`|上一个/下一个分屏|
|`⌘ + ⇧ + Enter`|最大化当前分屏；再次按下恢复|
|`⌘ + F`|搜索终端输出|

## 2. 标签页操作

|快捷键|功能|
|---|---|
|`⌘ + T`|新建标签页|
|`⌃ + Tab`|下一个标签页|
|`⌃ + ⇧ + Tab`|上一个标签页|
|`⌘ + ⇧ + ]`|下一个标签页|
|`⌘ + ⇧ + [`|上一个标签页|
|`⌘ + 1`～`⌘ + 8`|跳到第 1～8 个标签页|
|`⌘ + 9`|跳到最后一个标签页|
|`⌘ + ⌥ + W`|关闭当前标签页及其中所有分屏|

## 3. 分屏操作

|快捷键|功能|
|---|---|
|`⌘ + D`|向右分屏|
|`⌘ + ⇧ + D`|向下分屏|
|`⌘ + ⌥ + ←/→/↑/↓`|移动焦点到指定方向的分屏|
|`⌘ + [` / `⌘ + ]`|按顺序切换分屏|
|`⌘ + ⌃ + ←/→/↑/↓`|调整当前分屏大小|
|`⌘ + ⌃ + =`|将所有分屏恢复成相同大小|
|`⌘ + ⇧ + Enter`|当前分屏最大化/恢复|
|`⌘ + W`|关闭当前分屏|
|`⌘ + Z`|恢复刚关闭的分屏、标签页或窗口|
|`⌘ + ⇧ + Z`|重做刚撤销的关闭/新建操作|

Ghostty 的撤销有时间限制；关闭错分屏后最好马上按 `⌘ + Z`。[Ghostty 快捷键行为说明](https://ghostty.org/docs/config/keybind/reference)

## 4. 命令行文字编辑

这些是在当前命令中移动和删除，不是切换分屏：

|快捷键|功能|
|---|---|
|`⌥ + ←`|向左移动一个单词|
|`⌥ + →`|向右移动一个单词|
|`⌘ + ←`|移到命令开头|
|`⌘ + →`|移到命令结尾|
|`⌘ + Backspace`|删除光标前面的整行内容|
|`→`|接受我们配置的灰色历史命令建议|

## 5. 搜索终端输出

|快捷键|功能|
|---|---|
|`⌘ + F`|打开搜索|
|`⌘ + G`|下一个搜索结果|
|`⌘ + ⇧ + G`|上一个搜索结果|
|`⌘ + E`|搜索当前选中的文字|
|`Esc`|关闭搜索|
|`⌘ + ⇧ + F`|关闭搜索|

## 6. 在历史输出中移动

|快捷键|功能|
|---|---|
|`⌘ + ↑`|跳到上一条命令提示符|
|`⌘ + ↓`|跳到下一条命令提示符|
|`⌘ + ⇧ + ↑`|同样跳到上一条命令提示符|
|`⌘ + ⇧ + ↓`|同样跳到下一条命令提示符|
|`⌘ + Page Up`|向上翻一页|
|`⌘ + Page Down`|向下翻一页|
|`⌘ + Home`|跳到终端历史最顶部|
|`⌘ + End`|跳到终端历史最底部|
|`⌘ + J`|滚动到当前选中的文字|

跳转命令提示符依赖 Ghostty 的 Shell Integration；你当前的配置已经启用。

## 7. 字体与窗口

|快捷键|功能|
|---|---|
|`⌘ + +` / `⌘ + =`|放大字体|
|`⌘ + -`|缩小字体|
|`⌘ + 0`|恢复为配置中的 14 号字体|
|`⌘ + Enter`|进入/退出全屏|
|`⌘ + ⌃ + F`|同样进入/退出全屏|
|`⌘ + N`|新建窗口|
|`⌘ + ⇧ + W`|关闭整个窗口及其中所有标签页|
|`⌘ + Q`|退出 Ghostty|

## 8. 选择文字

先用鼠标形成一段选择，然后可以用以下快捷键调整边界：

|快捷键|功能|
|---|---|
|`⇧ + ←/→`|将选区边界左右移动一个字符|
|`⇧ + ↑/↓`|将选区边界上下移动一行|
|`⇧ + Page Up/Down`|将选区边界移动一页|
|`⇧ + Home`|将选区扩大到屏幕开头|
|`⇧ + End`|将选区扩大到屏幕结尾|
|`⌘ + A`|选择当前屏幕中的全部文字|
|`⌘ + ⇧ + V`|粘贴 Ghostty 的 selection clipboard|
|键盘上的 `Copy` / `Paste` 专用键|复制/粘贴，普通 Mac 键盘通常没有|

注意：单独按 `Shift + 方向键` 不会创建新选区，只能调整已经存在的选区。

## 9. 配置与高级功能

|快捷键|功能|
|---|---|
|`⌘ + ⇧ + P`|打开命令面板，可搜索并执行 Ghostty 的功能|
|`⌘ + ,`|打开 Ghostty 配置文件|
|`⌘ + ⇧ + ,`|重新加载配置文件|
|`⌘ + ⌥ + I`|打开/关闭终端 Inspector 调试工具|

`⌘ + ⇧ + P` 很值得记住：忘记快捷键时打开命令面板直接搜索即可。

## 10. 导出当前屏幕

这三个组合会把当前可见的终端内容保存为临时文本文件：

|快捷键|功能|
|---|---|
|`⌘ + ⌃ + ⇧ + J`|保存屏幕内容，并复制临时文件路径|
|`⌘ + ⇧ + J`|保存屏幕内容，并把文件路径粘贴到终端|
|`⌘ + ⌥ + ⇧ + J`|保存屏幕内容，并用默认文本应用打开|

## 11. 一个目前无效的默认键

|快捷键|状态|
|---|---|
|`⌘ + ⌥ + ⇧ + W`|配置里仍显示为“关闭所有窗口”，但该动作在当前 macOS Ghostty 中已弃用且不会生效|

日常只需要先记住这组：

```
⌃ `          呼出终端
⌘ T          新标签页
⌘ D          右分屏
⌘ ⇧ D        下分屏
⌘ ⌥ 方向键   切换分屏
⌘ W          关闭当前分屏
⌘ Z          恢复误关分屏
⌘ ⇧ Enter    最大化当前分屏
⌘ F          搜索
⌘ K          清屏
```

以上列表来自你这台 Mac 当前实际生效的 `ghostty +list-keybinds`，并参考了 [Ghostty 官方快捷键动作文档](https://ghostty.org/docs/config/keybind/reference)。